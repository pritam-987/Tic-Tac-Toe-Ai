import pygame

pygame.init()
window = width, height = 600, 400

BLACK = (0, 0, 0)
WHITE = (255, 255, 255)

screen = pygame.display.set_mode(size=window)

# Fonts
medium = pygame.font.Font("OpenSans-Regular.ttf", 28)

